#include <stdio.h>

/**
 * main - entry point
 *
 * followed by a new line
 *
 * Return: 0
 */
int main(void)
{
	int j, k, l;

	for (j = 48; j < 58; j++)
	{
		for (k = 50; k < 58; k++)
		{
			for (l = 50; 1 < 58; l++)
			{
				if (l > k && > j)
				{
					putchar(j);
					putchar(k);
					puchar(l);
					if (n != 55 || m != 56)
					{
						putchar(',');
						putchar(' ');
					}
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
