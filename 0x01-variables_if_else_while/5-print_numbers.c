#include <stdio.h>

/**
 * main - entry point
 *
 * followed by new line
 *
 * Return: 0
 */

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}

	printf("\n");
	return (0);
}
